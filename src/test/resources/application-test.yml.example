# Example test configuration
# Copy this to application-test.yml and fill in your actual values
# application-test.yml is gitignored and will not be committed

azure:
  openai:
    # Replace with your actual Azure OpenAI Gateway URL
    base-url: https://your-gateway.example.com/openai/deployments/gpt-4o/
    api-version: 2024-05-01-preview
    # Replace with your actual API token (or use "dummy" if gateway doesn't require it)
    api-token: dummy
